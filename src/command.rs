#[derive(Debug)]
pub enum Command {
    Version,
    Quit,
}

impl Command {
    pub fn remove_newline(s: &str) {}
}

// impl std::str::FromStr for Command {
//     fn from_str(s: &str) {

//     }
// }
